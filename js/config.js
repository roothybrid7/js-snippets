/**
 * config.js - Application configuration.
 */

/**
 * @type {Object}
 */
var appConfig = {};

(function(global) {
  'use strict';

  appConfig = {
    rootNs: 'App'
  };

  return appConfig;
}(this));
